# Optional / separately installable education-game dependency.
# It is also included in requirements.txt for the standard installer.
pyglet>=2.1,<3
# Legacy fallback renderer only; not used as the primary scenario engine.
pygame>=2.5,<3
